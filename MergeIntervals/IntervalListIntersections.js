function intervalsIntersection(intervalListA, intervalListB) {
  // Initialize variables -> intersections, i, j
  // Loop over both interval lists
  // Check for the later start time in both lists, store it start.
  // Check for the earlier end time in both lists, store it end.
  // If start time <= end time then push [start, end] into the intersections array
  // If intervalListA[i][1] < intervalListB[i][1] => i++
  // Else => j++;
}
